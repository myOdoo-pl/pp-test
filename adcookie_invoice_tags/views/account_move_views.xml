<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>

        <!-- Add tags field to account move view -->
        <record id="view_move_form_tags_ext" model="ir.ui.view">
            <field name="name">account.move.form.tags.ext</field>
            <field name="model">account.move</field>
            <field name="inherit_id" ref="account.view_move_form"/>
            <field name="arch" type="xml">
                <xpath expr="//div[@name='journal_div']" position="after">
                    <field name="category_id" widget="many2many_tags"
                           options="{'color_field': 'color', 'no_create_edit': True}" placeholder="Tags..."/>
                </xpath>
            </field>
        </record>

        <!-- Add tags field to account move list view -->
        <record id="view_invoice_tree_tags_ext" model="ir.ui.view">
            <field name="name">account.move.invoice.tree.tags.ext</field>
            <field name="model">account.move</field>
            <field name="inherit_id" ref="account.view_invoice_tree"/>
            <field name="arch" type="xml">
                <xpath expr="//field[last()]" position="after">
                    <field name="category_id" widget="many2many_tags" optional="show"/>
                </xpath>
            </field>
        </record>

        <!-- Category view -->
        <record id="view_account_move_category_form" model="ir.ui.view">
            <field name="name">Invoice Tags</field>
            <field name="model">account.move.category</field>
            <field name="arch" type="xml">
                <form string="Invoice Tag">
                    <sheet>
                        <group col="4">
                            <field name="name"/>
                        </group>
                        <br/>
                        <br/>
                        <field name="invoice_ids"/>
                    </sheet>
                </form>
            </field>
        </record>

        <!-- Categories search view -->
        <record id="account_move_category_view_search" model="ir.ui.view">
            <field name="name">account.move.category.view.search</field>
            <field name="model">account.move.category</field>
            <field name="arch" type="xml">
                <search string="Search Invoice Tags">
                    <field name="name"/>
                </search>
            </field>
        </record>

        <!-- Categories form view -->
        <record id="action_account_move_category_form" model="ir.actions.act_window">
            <field name="name">Invoice Tags</field>
            <field name="type">ir.actions.act_window</field>
            <field name="res_model">account.move.category</field>
            <field name="search_view_id" ref="account_move_category_view_search"/>
            <field name="help" type="html">
              <p class="o_view_nocontent_smiling_face">
                Create a new invoice tag
              </p>
            </field>
        </record>

    </data>
</odoo>